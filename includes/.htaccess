# Block all direct web access to BENUX Trading PHP includes.
# These files are loaded by the front controller, never browsed directly.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
